{
    "image": "python:3.11.1-bullseye",
    "postCreateCommand": "apt-get update && apt-get install -y wget jq libsqlite3-mod-spatialite && pip install datasette shapefile-to-sqlite geojson-to-sqlite"
}
